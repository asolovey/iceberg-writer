### Running

Parameters:

```
--bucket warehouse --catalog-uri jdbc:postgresql://localhost/iceberg_catalog --catalog-user admin --catalog-password password --table test --profile minio --s3-endpoint-url http://localhost:9000
```
